import fs from 'fs';

// function invokeAnything(callback: unknown) {
//     if (typeof callback === 'function') { //unknown заставляет проверять тип 
//         callback();
//     }
// }
// const show = () =>

//     invokeAnything(show);

//   ----------------------------------------------

const person1 = {
    name: "Danyil"
}

console.log(person1 instanceof Object) // проверяет person1 сделан ли с Object

//   ----------------------------------------------
